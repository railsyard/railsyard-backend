Railsyard::Backend.define_editor_for <%= @class_name %> do

  list do
    <%= "label :#{@fields.first[:name]}\n" unless @fields.empty? %>
    field :created_at
  end

  edit do
<% @fields.each do |f| -%>
    <%= f[:editor_config] %>
<% end -%>
  end

end
