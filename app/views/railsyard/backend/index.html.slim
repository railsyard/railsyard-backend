h2 = index_label(resource_class)

ul.collection data-sortable=editor_config.list.sorting_type data-sort-path=reorder_resources_path
  - collection.each do |resource|
    = render "resource", resource: resource
