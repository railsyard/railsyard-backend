source "http://rubygems.org"

# Declare your gem's dependencies in railsyard.gemspec.
# Bundler will treat runtime dependencies like base dependencies, and
# development dependencies will be added by default to the :development group.
gemspec

# These are required by dummy app
gem 'sqlite3'
gem 'slim-rails'
gem 'jquery-rails'
gem 'compass-rails'
gem 'sass-rails'
gem 'coffee-rails'
gem 'simple_form'
gem 'responders'
gem 'formalize-rails'
