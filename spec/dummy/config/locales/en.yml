en:
  activerecord:
    models:
      book:
        one: "Book"
        other: "Books"
    attributes:
      book:
        title: "Title"
        subtitle: "Subtitle"
        description: "Description"

  railsyard:
    blank: "---"
    helpers:
      list: "List %{model}"
      new: "New %{model}"
      edit: "Edit %{model}"
      destroy: "Delete %{model}"

    groups:
      defaults:
        default: "No group"
    group_hints:
      defaults:
        default: ""
